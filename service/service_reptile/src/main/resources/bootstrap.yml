spring:
  application:
    name: service-reptile
  cloud:
    nacos:
      config:
        server-addr: wlwl.work:8848 # nacos服务地址
        file-extension: yaml
        extension-configs:
          - data-id: RedisFeignNacos.yaml
            refresh: true
