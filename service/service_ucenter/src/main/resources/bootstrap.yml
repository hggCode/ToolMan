spring:
  application:
    name: service-ucenter
  cloud:
    nacos:
      config:
        server-addr: wlwl.work:8848 # nacos服务地址
        file-extension: yaml
        extension-configs:
          - data-id: mysql.yaml
            refresh: true
          - data-id: mail.yaml
            refresh: true